%article
  .spacer
  .signup
    .container_box
      - if flash[:error]
        = flash[:error]
      = form_tag("pints/confirm_redeem", :method => :post) do
        = password_field_tag "secret_input", nil, :size => 4, :maxlength => 4
        = submit_tag "Confirm Pint", :class => "button green phone_full_width"
