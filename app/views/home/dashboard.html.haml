%article
  .spacer
  .signup
    .container_box
      .container_header
        %h3 Who do you want to purchase a pint for?
      = form_tag("/home/search", :method => :get, :remote => true, :id => "twitter_search")
      = text_field_tag 'q'
      .spacer
      #search-results
      .container_footer
