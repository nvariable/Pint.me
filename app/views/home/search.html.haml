- @users.each do |u|
  %a(href="/buy/#{u.screen_name}")
    %span
      = image_tag u.profile_image_url, :size => "50x50"
      %h4
        = u.name
      %h5
        = link_to "@#{u.screen_name}", "http://twitter.com/#{u.screen_name}"
    %br
